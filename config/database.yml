defaults: &defaults
  adapter: mysql
  encoding: utf8
  host: 127.0.0.1
  pool: 5
  username: etamon
  password: etamon22
  
development:
  <<: *defaults
  database: etamon

test:
  <<: *defaults
  database: etamon

production:
  <<: *defaults
  database: etamon
